/* Meta-Stripper runtime configuration.
   Change these values without modifying app.js. */

"use strict";

window.META_STRIPPER_CONFIG = Object.freeze({
  maxFileBytes: 30 * 1024 * 1024,
  maxPixels: 50_000_000,
  maxBatchFiles: 20,
  jpegHeaderScanBytes: 2 * 1024 * 1024,
});
